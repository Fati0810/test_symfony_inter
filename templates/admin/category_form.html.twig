{% extends "admin/base.html.twig" %}

{% block main %}

	{{ form_start(categoryForm) }}
    <div class="form-group mt-2" style="margin-left: 10px">
    <div>
	{{ form_label(categoryForm.name, 'Nom de la catégorie : ') }}
	{{ form_widget(categoryForm.name) }}
    </div>
    <div>
	{{ form_label(categoryForm.description, 'Description de la catégorie : ') }}
	{{ form_widget(categoryForm.description) }}
    </div>

	{{ form_widget(categoryForm.Enregistrer) }}
    </div>


	{{ form_end(categoryForm) }}

{% endblock %}