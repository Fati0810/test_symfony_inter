{% extends 'admin/base.html.twig' %}

{% block main %}

{{form_start(productForm)}}
<div class="form-group mt-2" style="margin-left: 10px">
    <div > 
    {{form_label(productForm.name, 'Nom du produit :')}}
	{{form_widget(productForm.name)}}
    </div>

    <div>
    {{ form_label(productForm.price, 'Prix du produit :') }}
	{{ form_widget(productForm.price) }}
    </div>

    <div>
    {{ form_label(productForm.stock, 'Stock du produit :') }}
	{{ form_widget(productForm.stock) }}
    </div>

    <div>
    {{ form_label(productForm.description, 'Description du produit :') }}
	{{ form_widget(productForm.description) }}
    </div>

    <div>
    {{ form_label(productForm.category, 'Catégorie du produit :') }}
	{{ form_widget(productForm.category) }}
    </div>

    <div>
    {{ form_label(productForm.licence, 'Licence du produit :') }}
	{{ form_widget(productForm.licence) }}
    </div>
    <br>
    <div>
    {{ form_widget(productForm.Enregistrer) }}
    </div>
</div>

{{form_end(productForm)}}
{% endblock %}