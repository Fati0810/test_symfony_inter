{% extends "admin/base.html.twig" %}

{% block main %}

    <h1>{{ image.title }}</h1>
    <img src="{{ asset('media/' ~ image.src) }}" alt="{{ image.alt }}" class="a">

<style>
.a{
    width: 150px;
    height: 150px;
}

</style>
{% endblock %}